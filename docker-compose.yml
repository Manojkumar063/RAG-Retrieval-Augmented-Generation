services:
  rag:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    volumes:
      - ./documents:/app/documents
      - rag_data:/app/data
    restart: unless-stopped

volumes:
  rag_data:
